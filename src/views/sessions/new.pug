extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('session'), method: 'post' })
    +input('center', f, 'email')
    +input('center', f, 'password', { as: 'password' })
    +button(f)
