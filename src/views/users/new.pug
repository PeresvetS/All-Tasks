extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('users'), method: 'post' })
    +input('center', f, 'email')
    +input('center', f, 'firstName')
    +input('center', f, 'lastName')
    +input('center', f, 'password', { as: 'password' })
    +button(f)
